import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.nsd',
  appName: 'HelpApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
